export async function renderECOverview(container, portalState) {
  container.innerHTML = `
    <section class="card">
      <h3>E‑Campaigns Overview</h3>
      <p>Loading...</p>
    </section>
  `;

  // TODO: fetch stats, year selector, charts, etc.
}
