export async function renderECTopContacts(container, portalState) {
  container.innerHTML = `
    <section class="card">
      <h3>Campaigns</h3>
      <p>Loading...</p>
    </section>
  `;

  // TODO: fetch campaign list + expanders
}
