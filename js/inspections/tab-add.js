// inspections/tab-add.js

export async function renderInspectionAdd(container, portalState) {
    container.innerHTML = `
        <section class="card">
            <h3>Inspections – Add</h3>
            <p>(Add form will go here)</p>
        </section>
    `;
}
