// js/leads/tab-list.js
export async function renderLeadsList(container, portalState) {
  container.innerHTML = `
    <section class="card">
      <h2>Lead List</h2>
      <p>Filter bar + Add Lead button + table will go here.</p>
    </section>
  `;
}
