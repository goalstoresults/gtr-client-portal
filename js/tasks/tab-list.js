// tab-list.js
export function loadTasksList({ portalState, container }) {
  container.innerHTML = `
    <div class="card">
      <h3>Tasks — List</h3>
      <p>List placeholder (no logic yet)</p>
    </div>
  `;
}
